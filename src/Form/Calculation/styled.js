import styled from "styled-components";
import { theme } from "../styled";

export const Conclusion = styled.div`
  color: ${theme.colors.primaryColor};
  text-align: ${theme.text.position};
  margin-top: -16px;
`;
